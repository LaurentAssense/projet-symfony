<div class="custom-dropdown">
    <button class="custom-dropbtn">Téléchargements
        <i class="fa fa-caret-down"></i>
    </button>
    <div class="custom-dropdown-content">
        {% for image in images %}
            <a href="{{ path('app_image_download', {'filename': image}) }}">{{ image }}</a>
        {% endfor %}
    </div>
</div>